# Cloud sync (optional). Copy to .env.local and fill in to enable accounts +
# cloud-synced projects. Leave unset to run in local-only mode.
#
# Find these in your Supabase project: Settings -> API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
